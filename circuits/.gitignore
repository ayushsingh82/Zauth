node_modules
.env
.env.local

# 12MB prebuilt circom binary; install-on-demand in build.sh
bin/

# 18MB Phase-1 Powers of Tau; fetched on demand by build.sh
build/pot14_final.ptau

# transient build artifacts (regenerated by build.sh)
build/zauth_0.zkey
build/zauth.r1cs
build/zauth.sym
