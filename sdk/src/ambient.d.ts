// Upstream does not ship types. We only touch a narrow surface so we declare
// these modules as `any` rather than pulling in a full @types package.
declare module 'snarkjs';
declare module 'circomlibjs';
